{
	"name": "web3-app-with-vechain",
	"version": "1.0.0",
	"description": "This projects sets up a Vechain node and has a client and server to wotk with it",
	"main": "",
	"scripts": {
		"test": "",
		"server:watch": "cd ./server && tsc --watch",
		"dev:client": "cd ./client && npm run dev",
		"start:server:dev": "cd ./server && npm run dev",
		"dev": "concurrently \"npm:server:watch\" \"npm:dev:client\" \"npm:start:server:dev\""
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^7.6.0"
	}
}
